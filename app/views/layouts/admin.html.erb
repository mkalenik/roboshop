<!DOCTYPE html>
<html>
  <head>
    <title>Roboshop - Admin</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
      <div class="container-fluid">
        <a class="navbar-brand" href="<%= admin_root_path %>">Roboshop::Amin</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link <% if params[:controller] == "admin/products" %>active<% end %>"  aria-current="page" href="<%= admin_products_path %>">Produkty</a>
            </li>
            <li class="nav-item">
              <%= link_to admin_categories_path do %>
                <a class="nav-link  <% if params[:controller] == "admin/categories" %>active<% end %>" aria-current="page" href="<%= admin_categories_path %>">Kategorie</a>
              <% end %>
            </li>
          </ul>
        </div>
      </div>
    </nav>
  
    <div class="container-fluid">
      <%= yield %>
    </div>
  </body>
</html>
